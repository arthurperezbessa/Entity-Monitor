{
  "config": {
    "step": {
      "user": {
        "title": "Entity Monitor",
        "description": "Pick the integrations and/or entities to watch. Excluded entities are ignored entirely.",
        "data": {
          "integrations": "Integrations to monitor (every entity of these)",
          "only_primary_entity": "Watch only the main entity of each device",
          "entities": "Extra entities to monitor (individual)",
          "excluded_entities": "Entities to ignore",
          "seconds_threshold": "Confirm an outage after (seconds)",
          "coalesce_seconds": "Window to treat simultaneous drops as one (seconds)",
          "notify_service": "Notify service (e.g. notify.mobile_app_xxx) - optional",
          "central_url": "Central webhook URL (Home360 Feedback Central) - optional",
          "central_client_id": "Client id at the central (e.g. casa_silva) - optional",
          "central_token": "Client token at the central - optional",
          "n3_minutes_threshold": "N2 offline threshold (minutes of cumulative downtime, 0 disables)",
          "report_time_hour": "Daily report hour (0-23, local time)",
          "auto_reset_days": "Auto-reset cumulative statistics every N days (0 = never)"
        }
      }
    },
    "error": {
      "nothing_selected": "Pick at least one integration or one entity."
    },
    "abort": {
      "single_instance_allowed": "Entity Monitor is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Entity Monitor",
        "description": "Update the monitored integrations, entities and the notification thresholds.",
        "data": {
          "integrations": "Integrations to monitor (every entity of these)",
          "only_primary_entity": "Watch only the main entity of each device",
          "entities": "Extra entities to monitor (individual)",
          "excluded_entities": "Entities to ignore",
          "seconds_threshold": "Confirm an outage after (seconds)",
          "coalesce_seconds": "Window to treat simultaneous drops as one (seconds)",
          "notify_service": "Notify service (e.g. notify.mobile_app_xxx) - optional",
          "central_url": "Central webhook URL (Home360 Feedback Central) - optional",
          "central_client_id": "Client id at the central (e.g. casa_silva) - optional",
          "central_token": "Client token at the central - optional",
          "n3_minutes_threshold": "N2 offline threshold (minutes of cumulative downtime, 0 disables)",
          "report_time_hour": "Daily report hour (0-23, local time)",
          "auto_reset_days": "Auto-reset cumulative statistics every N days (0 = never)"
        }
      }
    },
    "error": {
      "nothing_selected": "Pick at least one integration or one entity."
    }
  },
  "services": {
    "generate_report": {
      "name": "Generate report",
      "description": "Build a downtime report ranking the entities and integrations that go offline the most."
    },
    "reset_statistics": {
      "name": "Reset statistics",
      "description": "Clear cumulative outage counters and downtime totals."
    },
    "reset_all": {
      "name": "Reset all data",
      "description": "Zero stats, integration state and ongoing-outage records."
    },
    "test_notification": {
      "name": "Send test notification",
      "description": "Fire a sample notification so you can confirm the notify service is set up correctly."
    }
  },
  "entity": {
    "button": {
      "test_notification": {
        "name": "Test notification"
      },
      "reset_all": {
        "name": "Reset all data"
      }
    }
  }
}
